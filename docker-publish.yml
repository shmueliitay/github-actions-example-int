# .github/workflows/hello.yml
name: Hello World

on:
  push:
    branches: [ "main" ]
jobs:
  say-hello:
    runs-on: ubuntu-latest
    steps:
      - name: Print message
        run: echo "Hello from GitHub Actions"